// Export the API_URL from api service for consistent usage
export { API_URL } from './services/api';

// Add other global configuration values here
export const APP_NAME = 'PrepAcademy';
export const DEFAULT_PAGINATION_LIMIT = 10; 